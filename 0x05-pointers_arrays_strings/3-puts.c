#include "main.h"

/**
 * _puts - check if c is upper
 *
 * @str: is var
 *
 * Return: print follwed by line
*/

void _puts(char *str)
{
	while (*s != '\0')
	{
		_putchar(*s + 0);
		s++;
	}
	_putchar('\n');
}
