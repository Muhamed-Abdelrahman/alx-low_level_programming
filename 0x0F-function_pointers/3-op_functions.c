#include "3-calc.h"

/**
 * op_add - check if c is upper
 *
 * @a: is var
 * @b: is var
 *
 * Return: print follwed by line
*/
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - check if c is upper
 *
 * @a: is var
 * @b: is var
 *
 * Return: print follwed by line
*/
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 * op_mul - check if c is upper
 *
 * @a: is var
 * @b: is var
 *
 * Return: print follwed by line
*/
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 * op_div - check if c is upper
 *
 * @a: is var
 * @b: is var
 *
 * Return: print follwed by line
*/
int op_div(int a, int b)
{

	return (a / b);
}
/**
 * op_mod - check if c is upper
 *
 * @a: is var
 * @b: is var
 *
 * Return: print follwed by line
*/
int op_mod(int a, int b)
{
	return (a % b);
}
